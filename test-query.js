import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function run() { console.log('Fetching...'); const { data, error } = await supabase.from('entries').select('*').limit(1); console.log('Result:', data, error); } run();  
